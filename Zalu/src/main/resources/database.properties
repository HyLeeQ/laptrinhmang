# Database Configuration
# Cấu hình kết nối MySQL cho Zalu Application

# JDBC URL - Thay đổi host/port nếu database không chạy trên localhost
db.url=jdbc:mysql://localhost:3306/laptrinhmang_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&connectTimeout=30000&socketTimeout=60000&autoReconnect=true&maxAllowedPacket=16777216

# Database credentials
db.username=root
db.password=

# JDBC Driver (không cần thay đổi)
db.driver=com.mysql.cj.jdbc.Driver

# Connection Pool Settings (tùy chọn, có thể override trong code)
# db.pool.maxSize=10
# db.pool.minIdle=2
# db.pool.connectionTimeout=30000
# db.pool.idleTimeout=600000
# db.pool.maxLifetime=1800000

